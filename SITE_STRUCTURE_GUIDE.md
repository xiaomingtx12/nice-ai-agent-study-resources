# Site Structure Guide

这份文档是给仓库维护者看的，不是站点正文。

目的只有一个：以后你想手动新增文档、调整导航、修改 sidebar 分组时，知道该改哪些文件，以及按什么规则改。

## 1. 哪些文件控制站点结构

### `docs/`

这里放所有站点正文。

- `docs/index.md`：首页
- `docs/about/index.md`：关于我
- `docs/resources/`：资源导航
- `docs/notes/`：方法与复盘
- `docs/application-notes/`：应用沉淀
- `docs/templates/`：共建与模板

你新增一篇文档，第一步永远是先把 `.md` 文件放到 `docs/` 下面对应的位置。

### `sidebars.ts`

这里控制左侧 sidebar。

这个项目现在不是“完全自动生成侧边栏”，而是“按栏目手写组织”：

- `resourcesSidebar`：手写分组和资源层级
- `notesSidebar`：手写扁平结构
- `applicationNotesSidebar`：手写分组
- `templatesSidebar`：手写流程文档和模板分组

如果你发现“文档文件已经存在，但左侧没显示出来”，优先检查这里。

### `docusaurus.config.ts`

这里控制顶栏导航、站点标题、GitHub 链接、`docs/` 挂载方式。

如果你想改：

- 顶栏顺序
- 顶栏文案
- 顶栏链接目标
- 站点标题 / tagline

就改这个文件。

### `src/css/custom.css`

这里放少量站点样式修正。

目前它主要负责：

- 首页和关于页宽度修正
- 少量默认样式覆盖

如果只是改文档组织，一般不需要动它。

### `scripts/validate-page-redesign.mjs`

这是一个轻量校验脚本，用来检查当前 sidebar 关键结构有没有被改坏。

适合在你改完 `sidebars.ts` 之后跑一遍。

## 2. 当前信息架构

### 顶栏

当前顶栏在 `docusaurus.config.ts` 里是：

- 首页
- 资源导航
- 方法与复盘
- 应用沉淀
- 共建与模板
- 关于我

### Sidebar 规则

#### 首页 / 关于我

没有 sidebar。

对应页面：

- `docs/index.md`
- `docs/about/index.md`

#### 资源导航

最上方先放介绍页：

- `resources/index`

下面按四个大类分组：

- 学习地图与路径
- Harness / Coding Agent
- 架构与生产化
- 源码与真实系统

每条资源都遵循同一个结构：

1. 资源主页 `index.md`
2. 锐评 `review.md`
3. 学习沉淀入口 `notes/index.md`
4. 学习沉淀下的若干笔记页

#### 方法与复盘

当前保持简单扁平：

- `notes/index`
- `notes/how-to-learn-agent-with-judgment`

#### 应用沉淀

最上方先放介绍页：

- `application-notes/index`

下面按组组织：

- 写作说明
- Dify 型应用
- 准备文档
- 历史备份

#### 共建与模板

前面是流程文档，后面是模板分组：

- `templates/index`
- `templates/how-to-add-resource`
- `templates/human-first-contribution`
- `templates/ai-assisted-contribution`
- 模板

## 3. 以后怎么新增文档

### 场景 A：给现有栏目新增一篇普通文档

例如你想给 `应用沉淀 -> Agent开放平台` 再加一篇文档。

步骤：

1. 在对应目录下新建 `.md`
2. 给文件起稳定的 slug 名，不要随便改路径
3. 打开 `sidebars.ts`
4. 把对应 doc id 手动加入目标分组的 `items`
5. 跑校验

比如你新增：

```text
docs/application-notes/dify-type-application/deployment-and-release.md
```

那你要在 `applicationNotesSidebar` 的 `Dify 型应用` 分组里补上：

```ts
'application-notes/dify-type-application/deployment-and-release'
```

### 场景 B：新增一条资源导航里的新资源

资源导航不是简单加一个文件，而是加一组文档。

建议目录结构：

```text
docs/resources/<resource-slug>/
├─ index.md
├─ review.md
└─ notes/
   ├─ index.md
   ├─ note-a.md
   └─ note-b.md
```

然后在 `sidebars.ts` 的 `resourcesSidebar` 里新增一个 `resourceEntry(...)`。

示例：

```ts
resourceEntry(
  '新资源名称',
  'resources/new-resource/index',
  'resources/new-resource/review',
  'resources/new-resource/notes/index',
  [
    'resources/new-resource/notes/note-a',
    'resources/new-resource/notes/note-b',
  ],
)
```

重点：

- `index.md` 是资源主页
- `review.md` 是锐评页
- `notes/index.md` 是学习沉淀入口
- 下面的数组是具体学习笔记页

如果你只建了文件夹，却没在 `sidebars.ts` 里补 `resourceEntry`，左侧不会按你想要的层级显示。

### 场景 C：新增一个新的栏目分组

例如你想在 `应用沉淀` 下再加一组 `MCP 实践`。

做法：

1. 先决定文档目录放在哪
2. 先把文档文件放进 `docs/`
3. 再到 `sidebars.ts` 里新增一个 `category`

示例：

```ts
{
  type: 'category',
  label: 'MCP 实践',
  collapsed: false,
  items: [
    'application-notes/mcp-practice/index',
    'application-notes/mcp-practice/tooling',
  ],
}
```

## 4. 什么时候需要改 `_category_.json`

只有在这两种情况下才需要：

1. 你使用了 `autogenerated` sidebar
2. 你希望目录本身有标签、排序或折叠配置

比如：

- `docs/application-notes/dify-type-application/_category_.json`
- `docs/application-notes/dify准备文档/_category_.json`
- `docs/application-notes/diyf-type-application-bac/_category_.json`

但要注意：

- 这个项目很多 sidebar 是手写的
- 手写 sidebar 时，最终显示顺序以 `sidebars.ts` 为准
- `_category_.json` 更像是目录自身的元数据，不是唯一真相

## 5. 最常改的地方

### 改顶栏

改 `docusaurus.config.ts` 里的 `themeConfig.navbar.items`

### 改左侧栏

改 `sidebars.ts`

### 改页面正文

直接改 `docs/` 下面对应的 `.md`

### 改首页 / 关于页宽度

改 `src/css/custom.css`

## 6. 修改后怎么验证

### 本地预览

```bash
npm run start
```

### 结构校验

```bash
node scripts/validate-page-redesign.mjs
```

### TypeScript 校验

```bash
npx tsc --noEmit
```

### 生产构建

```bash
npm run build
```

如果你在 Windows 上再次遇到这种报错：

```text
EPERM: operation not permitted, unlink ... server.bundle.js
```

先判断这是不是文件锁问题，不要第一时间怀疑 sidebar 写错了。

## 7. 当前约定

### 构建目录

- 正式产物目录只有 `build/`
- `build-verify*` 这类目录都是临时验证产物
- `.gitignore` 已经忽略了 `build-*/`

### 首页和关于页

- 作为纯文档页使用
- 不挂 sidebar

### 资源导航

- 不使用整目录自动展开
- 每条资源都显式写在 `sidebars.ts` 里

### 应用沉淀

- 介绍页永远放在最上面
- 后面再按主题分组

## 8. 一次改动的推荐顺序

以后你手动改文档组织，按这个顺序最稳：

1. 先改 `docs/` 里的文件和目录
2. 再改 `sidebars.ts`
3. 如果顶栏要变，再改 `docusaurus.config.ts`
4. 跑 `node scripts/validate-page-redesign.mjs`
5. 跑 `npm run start` 本地看实际效果

如果你改的是资源导航或应用沉淀，优先盯左侧栏是否出现这三类问题：

- 文档没显示
- 文档顺序不对
- 文档被摊平，而不是收在你想要的分类下面
