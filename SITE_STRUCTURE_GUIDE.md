# Site Structure Guide

这份文档是给仓库维护者看的，不是站点正文。

目的只有一个：以后你想手动新增文档、调整导航、修改 sidebar 分组时，知道该改哪些文件，以及按什么规则改。

## 1. 哪些文件控制站点结构

### `docs/`

这里放所有站点正文。

- `docs/index.md`：首页
- `docs/about/index.md`：关于我
- `docs/ai-relay/index.md`：AI 中转
- `docs/resources/`：资源导航
- `docs/notes/`：方法与复盘
- `docs/application-notes/`：应用拆解

你新增一篇文档，第一步永远是先把 `.md` 文件放到 `docs/` 下面对应的位置。

### `sidebars.ts`

这里控制左侧 sidebar。

这个项目现在三个 sidebar 都走 `autogenerated`，按目录自动生成：

- `resourcesSidebar`：自动生成，按 `docs/resources/` 目录结构
- `notesSidebar`：自动生成，按 `docs/notes/` 目录结构
- `applicationNotesSidebar`：自动生成，按 `docs/application-notes/` 目录结构

排序规则：分组的标签、位置、折叠状态由各目录下的 `_category_.json`（`label` / `position` / `collapsed`）控制；组内文档顺序由 frontmatter 的 `sidebar_position` 决定，没写才按文件名字母序兜底。

如果你发现“文档文件已经存在，但左侧没显示出来”，优先检查：文件是否在对应目录、frontmatter 是否完整——不需要改 `sidebars.ts`。

### `docusaurus.config.ts`

这里控制顶栏导航、站点标题、GitHub 链接、`docs/` 挂载方式。

如果你想改：

- 顶栏顺序
- 顶栏文案
- 顶栏链接目标
- 站点标题 / tagline

就改这个文件。

### `src/css/custom.css`

这里放少量站点样式修正。

目前它主要负责：

- 首页和关于页宽度修正
- 少量默认样式覆盖

如果只是改文档组织，一般不需要动它。

### `scripts/validate-page-redesign.mjs`

这是一个轻量校验脚本，用来检查当前 sidebar 关键结构有没有被改坏（三条 autogenerated 挂载、资源六组的 `_category_.json`）。

适合在你改完目录结构或分组配置之后跑一遍。

## 2. 当前信息架构

站点正文的三栏定位（资源导航是参考底座、应用拆解是主体、方法与复盘从中沉淀）见 `docs/index.md` 和 `AGENTS.md` 的「三栏的关系」一节；本文件只讲结构怎么改。

### 顶栏

当前顶栏在 `docusaurus.config.ts` 里是：

- 首页
- 资源导航
- 应用拆解
- 方法与复盘
- AI 中转
- 关于我

### Sidebar 规则

#### 首页 / 关于我 / AI 中转

没有 sidebar。

对应页面：

- `docs/index.md`
- `docs/about/index.md`
- `docs/ai-relay/index.md`

#### 资源导航

最上方先放介绍页：

- `resources/index`

下面按六个大类分组（每组是一个子目录，标签见各目录 `_category_.json`）：

- 通用 Agent 系统教程（`tutorials/`）
- 模式与生产参考（`references/`）
- AI 编程上手（`coding/`）
- Coding Agent 机制拆解（`mechanism/`）
- 真实系统源码（`systems/`）
- Harness Agent 框架（`harness-agent/`）

每条资源都是一个单页 `index.md`，不再拆 review 和 notes 子页：

- `resources/<group>/<slug>/index`（组内顺序用 frontmatter `sidebar_position`）

#### 方法与复盘

介绍页下面按主题分组：

- `notes/index`
- Agent 产品思维
- Agent 工程技巧
- 演变趋势观察
- 市场观察
- 工具应用与驾驭

#### 应用拆解

最上方是介绍页（已并入拆解方法论：人先判断价值点 → 按类型走骨架 → 三条底线）：

- `application-notes/index`

下面按拆解对象分组（都在 `engineering/` 下，另有 `product/` 占位区）：

- Dify 架构拆解（v1.15.0）：介绍页 + 16 篇正文
- Claude Code CLI 架构拆解：介绍页 + 18 篇正文
- LangChain：介绍页 + 正文（在建）
- Skill 编写应用案例拆解：介绍页 + 正文（在建）

## 3. 以后怎么新增文档

### 场景 A：给现有栏目新增一篇普通文档

例如你想给 `应用拆解 -> Dify 架构拆解` 再加一篇文档。

步骤：

1. 在对应目录下新建 `.md`
2. 给文件起稳定的 slug 名，不要随便改路径
3. 在 frontmatter 里写 `sidebar_position` 决定它在组内的位置
4. 跑校验

比如你新增：

```text
docs/application-notes/engineering/dify/dify-17-deployment-and-release.md
```

frontmatter 里写 `sidebar_position: 17`，构建后自动出现在 Dify 分组里，不需要动 `sidebars.ts`。

### 场景 B：新增一条资源导航里的新资源

资源导航现在每条资源就是一个单页 `index.md`。

建议目录结构：

```text
docs/resources/<group>/<resource-slug>/
└─ index.md
```

`<group>` 从六个分组目录里选一个：tutorials / references / coding / mechanism / systems / harness-agent。

重点：

- `index.md` 是资源单页，把定位、适合谁、为什么收录、怎么用都写在一页里
- frontmatter 写 `sidebar_position` 决定组内顺序、`description` 做一句话定位
- 文件放进分组目录就会自动出现在侧栏——**只建文件夹不需要补任何 sidebar 配置**
- 记得同步 `docs/resources/index.md` 的学习地图、阅读路线、资源角色卡

### 场景 C：新增一个新的栏目分组

例如你想在 `应用拆解` 下再加一组 `MCP 实践`。

做法（autogenerated 下分组就是目录）：

1. 新建目录 `docs/application-notes/engineering/mcp-practice/`（或你认为合适的层级）
2. 在目录里放 `_category_.json`：

```json
{
  "label": "MCP 实践",
  "position": 4,
  "collapsible": true,
  "collapsed": true
}
```

3. 往目录里放文档（含一张 `index.md` 介绍页，frontmatter `sidebar_position: 1`）

## 4. 什么时候需要改 `_category_.json`

三条侧栏全部 autogenerated，`_category_.json` 就是分组元数据的真相源。需要碰它的场景：

1. 新增分组目录时（写 `label` / `position` / `collapsed`）
2. 想改分组标签、调整分组顺序、改默认折叠状态时

现有的例子：

- `docs/resources/<group>/_category_.json`：六个资源分组的标签与排序
- `docs/notes/<group>/_category_.json`：方法与复盘五个主题分组
- `docs/application-notes/_category_.json` 及下层：栏目与项目分组

资源单页目录（`resources/<group>/<slug>/`）里不需要 `_category_.json`——单页只有 `index.md`，没有可展开的子项。

## 5. 最常改的地方

### 改顶栏

改 `docusaurus.config.ts` 里的 `themeConfig.navbar.items`

### 改左侧栏

改 `docs/` 目录结构本身：分组动 `_category_.json`，条目顺序动 frontmatter `sidebar_position`。`sidebars.ts` 只在新增一整条侧栏（几乎不会）时才动。

### 改页面正文

直接改 `docs/` 下面对应的 `.md`

### 改首页 / 关于页宽度

改 `src/css/custom.css`

## 6. 修改后怎么验证

### 本地预览

```bash
npm run start
```

### 结构校验

```bash
node scripts/validate-page-redesign.mjs
```

### TypeScript 校验

```bash
npx tsc --noEmit
```

### 生产构建

```bash
npm run build
```

如果你在 Windows 上再次遇到这种报错：

```text
EPERM: operation not permitted, unlink ... server.bundle.js
```

先判断这是不是文件锁问题，不要第一时间怀疑 sidebar 写错了。

## 7. 当前约定

### 构建目录

- 正式产物目录只有 `build/`
- `build-verify*` 这类目录都是临时验证产物
- `.gitignore` 已经忽略了 `build-*/`

### 首页和关于页

- 作为纯文档页使用
- 不挂 sidebar

### 资源导航

- 侧栏 autogenerated，分组即目录
- 每条资源是分组目录下的一个 `<slug>/index.md` 单页

### 应用拆解

- 介绍页永远放在最上面
- 后面再按主题分组

## 8. 一次改动的推荐顺序

以后你手动改文档组织，按这个顺序最稳：

1. 先改 `docs/` 里的文件和目录（含 `_category_.json`、frontmatter `sidebar_position`）
2. 如果顶栏要变，再改 `docusaurus.config.ts`
3. 跑 `node scripts/validate-page-redesign.mjs`
4. 跑 `npm run start` 本地看实际效果

如果你改的是资源导航或应用拆解，优先盯左侧栏是否出现这三类问题：

- 文档没显示
- 文档顺序不对
- 文档被摊平，而不是收在你想要的分类下面
