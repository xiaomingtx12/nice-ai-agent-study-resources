# Site Structure Guide

这份文档是给仓库维护者看的，不是站点正文。

目的只有一个：以后你想手动新增文档、调整导航、修改 sidebar 分组时，知道该改哪些文件，以及按什么规则改。

## 1. 哪些文件控制站点结构

### `docs/`

这里放所有站点正文。

- `docs/index.md`：首页
- `docs/about/index.md`：关于我
- `docs/resources/`：资源导航
- `docs/notes/`：方法与复盘
- `docs/application-notes/`：应用拆解

你新增一篇文档，第一步永远是先把 `.md` 文件放到 `docs/` 下面对应的位置。

### `sidebars.ts`

这里控制左侧 sidebar。

这个项目现在三个 sidebar 都走 `autogenerated`，按目录自动生成（顺序由文件名字母序决定）：

- `resourcesSidebar`：自动生成，按 `docs/resources/` 子目录与文件名字母序
- `notesSidebar`：自动生成，按 `docs/notes/` 子目录与文件名字母序
- `applicationNotesSidebar`：自动生成，按 `docs/application-notes/` 子目录与文件名字母序

子目录的标签和折叠状态由各目录下的 `_category_.json` 控制。

如果你发现“文档文件已经存在，但左侧没显示出来”，优先检查：文件是否在对应目录、文件名是否符合字母序预期。

### `docusaurus.config.ts`

这里控制顶栏导航、站点标题、GitHub 链接、`docs/` 挂载方式。

如果你想改：

- 顶栏顺序
- 顶栏文案
- 顶栏链接目标
- 站点标题 / tagline

就改这个文件。

### `src/css/custom.css`

这里放少量站点样式修正。

目前它主要负责：

- 首页和关于页宽度修正
- 少量默认样式覆盖

如果只是改文档组织，一般不需要动它。

### `scripts/validate-page-redesign.mjs`

这是一个轻量校验脚本，用来检查当前 sidebar 关键结构有没有被改坏。

适合在你改完 `sidebars.ts` 之后跑一遍。

## 2. 当前信息架构

站点正文的三栏定位（资源导航是参考底座、应用拆解是主体、方法与复盘从中沉淀）见 `docs/index.md` 和 `AGENTS.md` 的「三栏的关系」一节；本文件只讲结构怎么改。

### 顶栏

当前顶栏在 `docusaurus.config.ts` 里是：

- 首页
- 资源导航
- 应用拆解
- 方法与复盘
- 关于我

### Sidebar 规则

#### 首页 / 关于我

没有 sidebar。

对应页面：

- `docs/index.md`
- `docs/about/index.md`

#### 资源导航

最上方先放介绍页：

- `resources/index`

下面按四个大类分组：

- 学习地图与路径
- Harness / Coding Agent
- 架构与生产化
- 源码与真实系统

每条资源都是一个单页 `index.md`，不再拆 review 和 notes 子页：

- `resources/<slug>/index`（加 `_category_.json` 控制目录元数据）

#### 方法与复盘

介绍页下面按主题分组：

- `notes/index`
- 架构理解与入门
- Agent 系统设计
- Claude Code 应用
- 设计模式与元认知
- 实践落地
- 学习方法与复盘

#### 应用拆解

最上方是介绍页（已并入拆解方法论：人先判断价值点 → 按类型走骨架 → 三条底线）：

- `application-notes/index`

下面按拆解对象分组：

- Dify 架构拆解（v1.15.0）：介绍页 + 16 篇正文
- Claude Code CLI 架构拆解：介绍页 + 18 篇正文

## 3. 以后怎么新增文档

### 场景 A：给现有栏目新增一篇普通文档

例如你想给 `应用拆解 -> Dify 架构拆解` 再加一篇文档。

步骤：

1. 在对应目录下新建 `.md`
2. 给文件起稳定的 slug 名，不要随便改路径
3. 打开 `sidebars.ts`
4. 把对应 doc id 手动加入目标分组的 `items`
5. 跑校验

比如你新增：

```text
docs/application-notes/dify/dify-17-deployment-and-release.md
```

那你要在 `applicationNotesSidebar` 的 `Dify 架构拆解（v1.15.0）` 分组里补上：

```ts
'application-notes/dify/dify-17-deployment-and-release'
```

### 场景 B：新增一条资源导航里的新资源

资源导航现在每条资源就是一个单页 `index.md`。

建议目录结构：

```text
docs/resources/<resource-slug>/
├─ index.md
└─ _category_.json
```

然后在 `sidebars.ts` 的 `resourcesSidebar` 对应分组里，新增一行引用：

```ts
'resources/new-resource/index',
```

重点：

- `index.md` 是资源单页，把定位、适合谁、为什么收录、怎么用都写在一页里
- `_category_.json` 控制目录标签和折叠，可选
- 四个分组（学习地图与路径 / Harness · Coding Agent / 架构与生产化 / 源码与真实系统）选一个放进去

如果你只建了文件夹，却没在 `sidebars.ts` 里补这行，左侧不会显示这条资源。

### 场景 C：新增一个新的栏目分组

例如你想在 `应用拆解` 下再加一组 `MCP 实践`。

做法：

1. 先决定文档目录放在哪
2. 先把文档文件放进 `docs/`
3. 再到 `sidebars.ts` 里新增一个 `category`

示例：

```ts
{
  type: 'category',
  label: 'MCP 实践',
  collapsed: false,
  items: [
    'application-notes/mcp-practice/index',
    'application-notes/mcp-practice/tooling',
  ],
}
```

## 4. 什么时候需要改 `_category_.json`

只有在这两种情况下才需要：

1. 你使用了 `autogenerated` sidebar
2. 你希望目录本身有标签、排序或折叠配置

比如：

- `docs/application-notes/_category_.json`：栏目标签、位置和折叠配置
- `docs/about/_category_.json`：「关于我」分组的折叠配置
- `docs/resources/<slug>/_category_.json`：单条资源目录自身的元数据

但要注意：

- 这个项目很多 sidebar 是手写的
- 手写 sidebar 时，最终显示顺序以 `sidebars.ts` 为准
- `_category_.json` 更像是目录自身的元数据，不是唯一真相

## 5. 最常改的地方

### 改顶栏

改 `docusaurus.config.ts` 里的 `themeConfig.navbar.items`

### 改左侧栏

改 `sidebars.ts`

### 改页面正文

直接改 `docs/` 下面对应的 `.md`

### 改首页 / 关于页宽度

改 `src/css/custom.css`

## 6. 修改后怎么验证

### 本地预览

```bash
npm run start
```

### 结构校验

```bash
node scripts/validate-page-redesign.mjs
```

### TypeScript 校验

```bash
npx tsc --noEmit
```

### 生产构建

```bash
npm run build
```

如果你在 Windows 上再次遇到这种报错：

```text
EPERM: operation not permitted, unlink ... server.bundle.js
```

先判断这是不是文件锁问题，不要第一时间怀疑 sidebar 写错了。

## 7. 当前约定

### 构建目录

- 正式产物目录只有 `build/`
- `build-verify*` 这类目录都是临时验证产物
- `.gitignore` 已经忽略了 `build-*/`

### 首页和关于页

- 作为纯文档页使用
- 不挂 sidebar

### 资源导航

- 不使用整目录自动展开
- 每条资源都显式写在 `sidebars.ts` 里

### 应用拆解

- 介绍页永远放在最上面
- 后面再按主题分组

## 8. 一次改动的推荐顺序

以后你手动改文档组织，按这个顺序最稳：

1. 先改 `docs/` 里的文件和目录
2. 再改 `sidebars.ts`
3. 如果顶栏要变，再改 `docusaurus.config.ts`
4. 跑 `node scripts/validate-page-redesign.mjs`
5. 跑 `npm run start` 本地看实际效果

如果你改的是资源导航或应用拆解，优先盯左侧栏是否出现这三类问题：

- 文档没显示
- 文档顺序不对
- 文档被摊平，而不是收在你想要的分类下面
