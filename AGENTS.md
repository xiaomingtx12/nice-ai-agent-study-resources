# AGENTS.md

这个仓库是「Nice AI 学习沉淀」站点，用 Docusaurus 搭成，部署在 GitHub Pages。

它不是一个收藏夹。站点分三栏：应用拆解带真实问题看开源项目怎么解决、方法与复盘从中抽出能带到下一次的判断和 agent 工程技巧、资源导航是拆解和复盘卡住时随时可查的参考底座。方法与复盘的技巧目标封成 skill 回喂应用拆解，让工程判断越攒越准。skill 那一步现在还没落地，别把它当成已完成的事实来写。

给在这里工作的代理的约定很简单：先看现有结构和现有风格，再按三栏定位改，不要把仓库改成别的样子。

## 先读哪些文件

- `README.md`：仓库定位、内容范围、本地预览方式
- `docs/index.md`：首页，站点分工与三栏关系的论述出处
- `SITE_STRUCTURE_GUIDE.md`：站点结构、侧栏规则、常见修改路径
- `docusaurus.config.ts`：顶栏、站点信息、docs 挂载方式
- `sidebars.ts`：三条侧栏的挂载点（均为 autogenerated，指向 `docs/` 下对应目录）

## 三栏的关系

站点分三栏，但它们不是平级的三个环节：

- **应用拆解**（`docs/application-notes/`）：人先判断这个项目值得拆哪一点（工程实现 / 功能交互 / 商业模式），定下方向，AI 再沿方向深挖，人验真伪。
- **方法与复盘**（`docs/notes/`）：从应用拆解里提炼能带到下一次的判断、模式和 agent 工程技巧。目标是封成 skill 回喂应用拆解，让工程判断越攒越准。skill 那一步现在还没落地，别把它写成事实。
- **资源导航**（`docs/resources/`）：拆解和复盘卡住时随时可查的参考底座。它不在前面那条回线上。

描述三栏关系时别硬把它们编成对称闭环来凑好看——资源导航跟其他两栏的关系是"按需查"，不是"流转"。

## 这个仓库的内容结构

- `docs/resources/`：资源导航区，11 条资源分五组（通用 Agent 系统教程 / 模式与生产参考 / AI 编程上手 / Coding Agent 机制拆解 / 真实系统源码），每组是一个子目录
- `docs/application-notes/`：带着真实问题拆开源项目源码与架构，现有 Dify v1.15.0（16 篇）、Claude Code CLI（18 篇）、LangChain（在建）三组，外加 product 占位区
- `docs/notes/`：回喂应用拆解的一栏——跨项目还能复用的判断、模式和技巧，目标是封成 skill
- `docs/about/`：站点作者和写作背景说明
- `src/`：站点主题、搜索页、样式和少量交互组件
- `scripts/`：结构校验和构建辅助脚本

## 资源导航怎么写

每条资源是一个单页 `index.md`，把定位、适合谁、为什么收录、怎么用都写在一页里，不再拆 review 和 notes 子页。

标准结构是：

```text
docs/resources/<group>/<resource-slug>/
  index.md
```

`<group>` 是五个分组目录之一（tutorials / references / coding / mechanism / systems），分组标签、排序、折叠由该目录的 `_category_.json` 控制。资源在组内的顺序由 `index.md` frontmatter 的 `sidebar_position` 决定。

侧栏是 autogenerated：文件放进分组目录就会自动出现，**不需要改 `sidebars.ts`**。新加资源后如果侧栏没出现，先检查文件是否在正确的分组目录、frontmatter 是否完整。

新增资源后，通常还要同步更新 `docs/resources/index.md` 里的：

- 学习地图
- 常见阅读路线
- 资源角色卡

## 应用拆解怎么写

不套万能模板。方法分两层，完整版在 `docs/application-notes/index.md`：

1. **人先判断这个项目值得拆哪一点**：工程实现做得好 / 某个功能交互做得绝 / 商业模式与 AI 落点。每次只沿一个方向拆，贪多就同质化。这一步 AI 替不了。
2. **按价值点类型走对应的拆解骨架**：拆工程实现和拆商业模式问的问题不同，骨架按类型复用、不按项目复用。
3. AI 沿方向深挖、铺广度，人验真伪再定稿。

三条底线（每篇都守）：

- 不抄 README，每篇至少指明 3 处 README 没写、只有源码才看得出的东西
- 不把源码翻成大白话，要指出位置、约束、代价
- 必须给工程判断：哪些照搬、哪些换实现、哪些别碰、什么时候不适用

现有三组沉淀（Dify / Claude Code CLI / LangChain）都是「工程实现」类，功能交互和商业模式两类还没有样本。

Claude Code CLI 是闭源商业工具，正文里的 `src/...` 是逆向恢复的内部模块路径，仅标位置、不可点击，别把它当成可访问的仓库路径。

## 文档风格

底线一条：**务实，每句都要有信息量。** 删掉所有不承载信息的话——铺垫、过渡、总结性的重复、"得益于……""在当今……"这类开场白、"总而言之""希望对你有帮助"这类收尾。一句话如果去掉后读者不会少任何判断，就去掉它。

- 全站默认中文写作
- 语气要像判断，不像宣传；先讲结论，再讲理由
- 不写口水话：不堆"很重要""很值得""非常强大"这类空判断，不写正确的废话，不为凑字数展开
- 描述站点、资源、栏目「是什么」，避开「让你/帮你/使你怎么怎么」这类把读者动作当卖点的写法
- 不要把目录复述成正文，不要用小标题占位却没内容
- 兑现不了的事别写成已完成：skill、工作流、商业项目线还没落地，写成"目标/在建"，不写成事实
- 资源页要说清楚适合谁、不适合谁、为什么收录、怎么用，值不值得投入时间的判断直接写进同一页
- 方法与复盘要保留真实判断和复盘，不要变成目录复印件或流水账

## 常见页面约定

### `docs/resources/<slug>/index.md`

写资源单页，通常包含：

- 官网 / 仓库链接
- 这条资源在讲什么
- 适合谁 / 不太适合谁
- 我为什么把它收进来
- 建议怎么用
- 版权说明

适合谁、为什么收录、怎么用都要落到明确判断，不要堆“很重要”“很值得”。锐评不再是单独一页，判断直接写进上面这几段。

## 侧栏和结构

- 三条侧栏（resources / notes / application-notes）全部 autogenerated，`sidebars.ts` 只做挂载，不列条目
- 侧栏真相源是目录结构本身：分组标签/排序/折叠看各目录 `_category_.json`，条目顺序看 frontmatter `sidebar_position`
- 新增文档不需要改 `sidebars.ts`；侧栏没出现先查目录位置和 frontmatter
- 首页和关于页是纯文档页，不挂 sidebar

## Markdown 链接维护

文档之间互相引用的相对路径、锚点 slug，是结构健康最容易烂的地方。`onBrokenLinks` 和 `onBrokenAnchors` 都设成 `'throw'`，构建会拦住，但靠构建才发现意味着坏链接已经混进 git history。

**改目录结构、搬文件、改 heading 之前，先扫一遍引用面**：

- 改 `docs/` 任意目录的层级或文件名 → `grep -rE "\\]\\(\\./|\\]\\(\\.\\./|\\]\\(#" docs/` 看哪些文件引用了它
- 改任何 heading（一/二/三级标题）→ 用 `github-slugger` 重新算 slug，文中所有 `#xxx` 引用同步改。中文 heading 的 slug 规则特殊：`六、回放恢复：多分支对话` → `六回放恢复多分支对话`（`、``：`等标点和后续中文直接相连，**不**自动加 `-`）
- 加新文档 → 如果它会被其它文档引用，记得在新文档里也写链接（双向引用比单向更耐改）

**写链接时**：

- 相对路径深度要数清楚。`docs/notes/agent-system-design/foo.md` 引用 `docs/application-notes/engineering/dify/bar.md` 是 `../../application-notes/engineering/dify/bar.md`，**不是** `../application-notes/engineering/dify/bar.md`
- 引用站内资源用相对路径，不要写绝对 URL（部署到子路径时会失效）
- 引用本站点之外的源码（如 LangChain 源码 `libs/langchain_v1/...`）写成纯文字行号（`factory.py:942-1056`），不要写成 markdown 链接——Docusaurus 会把每个非站内链接都当 broken 查

**改完之后**：

```bash
npm run build  # onBrokenLinks + onBrokenAnchors 都设成 throw，build 失败就是有坏链接
```

构建失败不要第一时间去怀疑内容逻辑——`grep "Exhaustive\|couldn\|Broken" build.log` 找源头。

## 前端和样式

- `src/` 里的实现以现有 React / TypeScript / CSS Modules 风格为准
- `custom.css` 只做少量全站样式修正，不要把它变成大杂烩
- 优先复用现有主题、搜索和布局做法，少加依赖，少做跨文件重构
- 如果要改视觉，保持站点现在这种克制、密度高、偏编辑型的风格

## 修改时的习惯

- 先读相邻的已有文件，再改同类文件
- 只改和当前任务有关的文件
- 不要回滚工作区里不是你改的内容
- 路径、slug、sidebar id 要保持稳定，别频繁重命名

## 验证

常用检查顺序：

```bash
node scripts/validate-page-redesign.mjs
npm run build
npm run start
```

改完 markdown 链接或目录结构后，必须跑 `npm run build` 确认无 broken link / anchor（构建配置里两者都设成 `throw`）。

如果你改的是某个资源区，优先再跑对应的结构校验脚本。

如果在 Windows 上构建时报 `EPERM`、`unlink`、`rename` 一类错误，先判断是不是文件锁或环境问题，再怀疑文档内容本身。

## 变更优先级

1. 先保证站点结构和侧栏正确
2. 再保证资源页、应用拆解页内容和语气一致
3. 最后才考虑样式和结构优化
